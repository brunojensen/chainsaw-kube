apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "chainsaw-config-server.fullname" . }}-test-connection"
  labels:
{{ include "chainsaw-config-server.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "chainsaw-config-server.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
