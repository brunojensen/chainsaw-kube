apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "chainsaw-bank.fullname" . }}-test-connection"
  labels:
{{ include "chainsaw-bank.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "chainsaw-bank.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
