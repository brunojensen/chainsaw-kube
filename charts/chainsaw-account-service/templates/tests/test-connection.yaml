apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "chainsaw-account.fullname" . }}-test-connection"
  labels:
{{ include "chainsaw-account.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "chainsaw-account.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
