apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "chainsaw-quarkus-example.fullname" . }}-test-connection"
  labels:
{{ include "chainsaw-quarkus-example.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "chainsaw-quarkus-example.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
